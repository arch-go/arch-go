ignore:
- "**/mocks/**"
- "**/*_test.go"
- "internal/verifications/naming/test/**"
coverage:
  status:
    # we've found this not to be useful
    patch: off
    project:
      default:
        # allow test coverage to drop by 1%, assume that it's typically due to CI problems
        threshold: 1
