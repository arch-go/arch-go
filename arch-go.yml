dependenciesRules:
  - package: "**.cmd.*"
    shouldOnlyDependsOn:
      - "**.arch-go.*"
      - "*.bar.*"
    shouldNotDependsOn: ["**.model.**"]
  - package: "**.impl.**"
    shouldOnlyDependsOn:
      - "**.arch-go.**"
