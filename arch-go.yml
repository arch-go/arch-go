dependenciesRules:
  - package: "**.cmd.*"
    shouldOnlyDependsOn:
      - "**.arch-go.**"
  - package: "**.impl.**"
    shouldOnlyDependsOn:
      - "**.arch-go.**"

contentsRules:
  - package: "**.impl.model"
    shouldNotContainInterfaces: true
  - package: "**.impl.config"
    shouldOnlyContainFunctions: true
  - package: "**.impl.dependencies"
    shouldNotContainTypes: true
    shouldNotContainInterfaces: true
    shouldNotContainMethods: true

cyclesRules:
  - package: "**.cmd"
    shouldNotContainCycles: true
